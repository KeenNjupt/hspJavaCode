1. 插入的数据值应与字段的数据类型相同，如果类型不同数据库会进行隐式类型转换，如果无法转换就会报错
2. 数据长度应在字段类型的长度范围内
3. 应按照语句中的字段顺序插入对应的值
4. 字符和日期类型应该包裹在单引号中
5. 字段值可以为插入null，前提为创建表时没有限制该字段值not null
6. 插入多条记录：insert into table_name(field1, filed2) values(), ()...
7. 如果字段设置的默认值 default xx, 则不给该字段添加数据，该字段会填充默认值，如果没有指定默认值，默认值为null